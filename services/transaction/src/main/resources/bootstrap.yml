spring:
  application:
    name: transaction
  cloud:
    config:
      name: common,transaction

server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:2222/eureka}
  instance:
    preferIpAddress: true
