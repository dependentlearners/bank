spring:
  application:
    name: report-batch
  cloud:
    config:
      name: common,report-batch

server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:2222/eureka}
  instance:
    preferIpAddress: true
