spring:
  application:
    name: service-admin

server:
  port: 3333

info:
  version: 1.0.0
  type: Tool
  usage: Spring boot admin

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:1111/eureka}
  instance:
    preferIpAddress: true

security:
  user:
    name: admin
    password: admin123

management:
  security:
    enabled: false

