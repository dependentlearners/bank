spring:
  application:
    name: mobile-api
  cloud:
    config:
      name: common,mobile-api

server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:2222/eureka}
  instance:
    preferIpAddress: true
